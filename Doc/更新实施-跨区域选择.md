# 更新实施文档:跨区域选择(多选拼段翻译)

> 版本:v1.0
> 日期:2026-08-13
> 状态:设计已收敛(grilling 会话逐轮确认),待实施
> 目标版本:v0.5.0
> 配套:[`PLAN.md`](./PLAN.md) 为总方案(本文档是独立的功能更新实施规格,不修改 PLAN.md)

---

## 1. 背景与需求

**问题**:PDF 文献中同一段落的内容往往不连续——双栏排版、页脚注、跨页、图注框都会把一段文字拆散。单次划选只能选中其中一块,翻译结果段落残缺。

**目标**:允许用户把多个不连续区域累积为一次翻译的原文,合成完整段落,走现有翻译管线。

**约束**:不改变既有单段翻译行为;默认状态零 UI 占用;基于官方公开事件,不侵入 pdf.js 内部。

---

## 2. 设计决策(已确认)

| # | 维度 | 决策 |
|---|------|------|
| D1 | 入口 | 划选弹层新增「加入选区」按钮,与「翻译」并列;点击取消挂起的划选即译定时器,按钮短暂显示「✓ 已加入(N段)」后恢复,弹层保持,可继续划下一段 |
| D2 | 去重 | 重复区域按「页+位置」静默去重,按钮短暂提示「已在选区中」 |
| D3 | 容器 | 侧栏新增「跨区域选区」区块,**默认 `setEnabled(false)` 完全隐藏(含标题栏,零占用)**;加入第一段时激活出现,清空后消失 |
| D4 | 布局 | 紧凑:头部一行「翻译全部 / 清空」+ 区域行「页码 + 文本预览(截断) + ×删除」;点击行 `reader.navigate({ pageIndex })` 跳页核对 |
| D5 | 排序 | 区域按文档顺序自动排序:`pageIndex` 升序,同页按 `sortIndex` 升序;与用户添加顺序无关 |
| D6 | 拼接 | 以空格拼接合成一段完整原文,走现有管线(格式化→分块→渠道→历史),记为一条历史 |
| D7 | 生命周期 | 内存级 `Map<itemID, Region[]>`;切换标签页/文献不丢(按文献隔离);手动「清空」重置;翻译后**不**自动清空(便于调参重译);上限 50 段;关闭 Zotero 后自然消失 |
| D8 | v1 不做 | 页内高亮覆盖、数据库持久化、Ctrl+拖拽追加(入 v2) |

---

## 3. 已核实的技术事实(源码级)

| 事实 | 来源 |
|------|------|
| `renderTextSelectionPopup` 事件的 `params.annotation` 含:`text`、`position.pageIndex`、`position.rects`、`sortIndex`(页内文档序)、`pageLabel`(如 "p. 3")、`type`/`color` | zotero/reader `src/pdf/pdf-view.js` `_getAnnotationFromSelectionRanges()`(master 分支) |
| 事件最多暴露**前两页**选区(`selectionRanges.slice(0, 2)`),跨 3+ 页的单次拖选文本会被截断 | 同上 |
| `ItemPaneManager` 区块的 `setEnabled` 实现为 `setEnabled: enabled => this.hidden = !enabled`,禁用时**整个区块(含标题栏)完全隐藏** | zotero/zotero `chrome/content/zotero/elements/itemPaneCustomSection.js:202` |
| `ReaderInstance.navigate(location)` 为公开 API,传 `{ pageIndex }` 跳页 | zotero/zotero `chrome/content/zotero/xpcom/reader.js:751` |
| 区块渲染驱动模式(本项目):`onInit` 订阅任务变化、`onItemChange` 更新数据 + `setEnabled`、`onRender` 初始渲染;XUL 文档中元素须显式 `html:` 前缀 | 本项目 `src/ui/sections.ts`(已按官方文档语义实现) |

---

## 4. 现状分析(相关代码)

| 文件 | 现状 | 本次改动 |
|------|------|----------|
| `src/modules/reader.ts` | `registerSelectionPopup()` 在弹层注入「翻译」按钮 + 可选划选即译(防抖定时器 `autoDebounceTimer`) | 追加「加入选区」按钮逻辑;导出 `getCurrentReader()` 供跳页复用 |
| `src/ui/sections.ts` | `registerSections()` 注册两个区块(reader/item);`contexts` Map 按 body 追踪;`onItemChange` 用 `setEnabled` 控制显隐 | 新增第三个区块「跨区域选区」(仅 `tabType==='reader'`) |
| `src/modules/tasks.ts` | `TranslateManager.translate({ sourceText, context?, itemID?, channelId? })`;单消费者队列;`subscribe()` 驱动结果卡片 | 无改动(翻译全部直接复用) |
| `src/modules/formatter.ts` | 断行合并、连字符断词修复等纯函数管线 | 可选:拼接时补一条「区域边界连字符」微修复(见 §6.5) |
| `src/addon.ts` / `src/hooks.ts` | 装配 `ReaderModule`/`registerSections`/`registerSelectionPopup` | 新增 SelectionManager 装配 |
| `addon/locale/{zh-CN,en-US}/mainWindow.ftl` | 弹层/区块文案 | 新增 §6.8 文案 |

---

## 5. 数据模型

```ts
// src/modules/selection.ts(新增)
export interface SelectionRegion {
  /** 添加顺序号(展示序号,从 1 起) */
  seq: number;
  /** 页码(0 基,来自 annotation.position.pageIndex) */
  pageIndex: number;
  /** 页内文档序(来自 annotation.sortIndex) */
  sortIndex: number;
  /** 页签文案(来自 annotation.pageLabel,如 "p. 3") */
  pageLabel: string;
  /** 区域原文 */
  text: string;
  /** 去重键:pageIndex + sortIndex */
  key: string;
}

export class SelectionManager {
  private regions = new Map<number, SelectionRegion[]>();
  private listeners = new Set<() => void>();
  static readonly MAX_REGIONS = 50;

  /** 加入区域;已存在(同 key)返回 { added: false, reason: 'dup' };超上限返回 { added: false, reason: 'limit' } */
  add(itemID: number, region: Omit<SelectionRegion, 'seq' | 'key'>): { added: boolean; reason?: 'dup' | 'limit'; count: number };
  remove(itemID: number, key: string): void;
  clear(itemID: number): void;
  /** 按文档顺序返回排序后的副本 */
  get(itemID: number): SelectionRegion[];
  has(itemID: number): boolean;
  /** 区块订阅:add/remove/clear 后触发,区块据此 setEnabled + 重绘 */
  subscribe(listener: () => void): () => void;
}
```

排序比较器:`pageIndex` 升序 → `sortIndex` 升序(区域 `seq` 仅作展示,不参与排序)。

---

## 6. 技术方案

### 6.1 装配

`src/addon.ts`:新增 `selection: new SelectionManager()` 注入 `ReaderModule`(构造参数扩展)与 `registerSections`(新参数);`hooks.ts` 在 `reader.registerSelectionPopup()` 处同步传入。

### 6.2 弹层改造(`src/modules/reader.ts`)

`registerSelectionPopup()` 事件回调中,现有「翻译」按钮旁追加「加入选区」按钮:

```
流程:划选 → 弹层出现 [翻译] [加入选区] → 点「加入选区」:
  1. 若 autoDebounceTimer 挂起 → clearTimeout(取消划选即译)
  2. annotation 无 text → 直接 return(现有守卫已处理)
  3. selection.add(logicalItemID(reader.itemID), {
       pageIndex: annotation.position?.pageIndex ?? 0,
       sortIndex: annotation.sortIndex ?? 0,
       pageLabel: annotation.pageLabel ?? `p. ${pageIndex + 1}`,
       text: annotation.text,
     })
  4. 按钮反馈:
     - added  → 文案「✓ 已加入(N段)」,800ms 后恢复「加入选区」
     - dup    → 文案「已在选区中」,800ms 后恢复
     - limit  → 文案「已达上限(50段)」,800ms 后恢复
  5. 弹层保持(不改变选区),用户继续划下一段
```

细节:
- 按钮间 `gap: 4px` 与现有容器样式一致
- 无 `position`/`sortIndex` 字段时(极端兼容):`pageIndex=0`、`sortIndex=seq`(退化为按添加顺序排序,仅作兜底)
- 导出 `getCurrentReader()`(改为 public/导出),供区块跳页复用

### 6.3 区块注册(`src/ui/sections.ts`)

第三个 `registerSection`,仅 `tabType === 'reader'` 启用:

```ts
Zotero.ItemPaneManager.registerSection({
  paneID: 'translator-selection',
  pluginID,
  header: { l10nID: 'ztr-section-selection-header', icon: <section 图标复用> },
  bodyXHTML: `...<div id="ztr-selection-toolbar"></div>
              <div id="ztr-selection-list"></div>`,
  onInit: ({ body, refresh }) => { /* 保存 ctx + setEnabled;订阅 SelectionManager */ },
  onItemChange: ({ body, item, tabType, setEnabled }) => {
    ctx.itemID = item?.id ?? null;
    // 激活条件:当前文献有选区且处于阅读器 tab
    setEnabled(ctx.itemID != null && selection.has(ctx.itemID) && tabType === 'reader');
    renderSelectionList(ctx);
  },
  onRender: /* 初始渲染列表 */,
});
```

**激活/隐藏时机**(关键):
- `SelectionManager` 订阅回调:add/remove/clear 后 → 找到对应 ctx(按 `ctx.itemID` 匹配,无匹配则跳过)→ `setEnabled(有选区)` + `renderSelectionList(ctx)`
- `onItemChange`:切换文献/窗格时按上述激活条件刷新
- 无选区 → `setEnabled(false)` → 整区块(含标题栏)完全隐藏,零占用

**区块内容**:
- 工具栏:「翻译全部」「清空」按钮(小号,复用 `ztr-btn`/`ztr-icon-btn` 类)
- 区域列表:每行 = `① p.3 文本预览(单行截断,title 全文) ×(删除)`;行点击 → `Zotero.Reader.getByTabID(win.Zotero_Tabs.selectedID)?.navigate({ pageIndex })`
- 列表项数量 ≥ 2 时行首显示文档顺序序号(`①②③…`,按排序后位置)
- 「翻译全部」:拼接(§6.5)→ `translateManager.translate({ sourceText: joined, itemID: ctx.itemID, channelId: getSelectedChannelId() })` → 结果自然出现在现有「当前翻译」卡片(同一 `subscribe` 驱动),无需额外改动

### 6.4 生命周期与边界

- 内存级 Map,关闭 Zotero 自然清空;切换文献不丢
- 上限 50:add 拒绝 + 按钮提示(§6.2 step 4)
- 空选区 → 区块隐藏,无空态页面(激活瞬间即有内容)
- 区块渲染时对 `ctx.itemID == null` 一律隐藏(与现有「绝不回退全局」原则一致)

### 6.5 拼接(`translateAll`)

```ts
function joinRegions(regions: SelectionRegion[]): string {
  const sorted = [...regions].sort((a, b) =>
    a.pageIndex - b.pageIndex || a.sortIndex - b.sortIndex);
  let out = '';
  for (const r of sorted) {
    const t = r.text.trim();
    if (!t) continue;
    if (!out) { out = t; continue; }
    // 区域边界连字符修复:前段以 "-" 结尾且后段以字母开头 → 直接相连
    if (/[A-Za-z]-$/.test(out) && /^[a-z]/.test(t)) {
      out = out.slice(0, -1) + t;
    } else {
      out += ' ' + t;
    }
  }
  return out;
}
```

- 拼接结果作为 `sourceText` 送入现有管线;格式化(断行合并/连字符修复)继续作用于合成文本
- 记一条历史(现有落库逻辑,无需改动)

### 6.6 与既有功能的关系

| 既有功能 | 影响 |
|----------|------|
| 单段翻译(弹层「翻译」) | 不变,独立于选区 |
| 划选即译 | 「加入选区」点击取消挂起定时器,不触发自动翻译;其余场景不变 |
| 快捷键翻译/总结 | 不变(仍作用于最近一次划选文本/任务) |
| 历史/缓存/总结 | 翻译全部走同一管线,记为一条历史;总结入口对结果卡片自动生效 |

### 6.7 目录与文件清单

| 文件 | 动作 |
|------|------|
| `src/modules/selection.ts` | 新增(SelectionManager + joinRegions) |
| `src/modules/reader.ts` | 修改(加入选区按钮、导出 getCurrentReader) |
| `src/ui/sections.ts` | 修改(第三区块注册 + 渲染) |
| `src/addon.ts` / `src/hooks.ts` | 修改(装配 SelectionManager) |
| `addon/locale/zh-CN/mainWindow.ftl` | 新增文案 |
| `addon/locale/en-US/mainWindow.ftl` | 新增文案 |

### 6.8 i18n 文案清单

| key | zh-CN | en-US |
|-----|-------|-------|
| `ztr-add-to-selection` | 加入选区 | Add to selection |
| `ztr-added-to-selection` | ✓ 已加入({$count}段) | ✓ Added ({$count}) |
| `ztr-already-in-selection` | 已在选区中 | Already in selection |
| `ztr-selection-limit` | 已达上限(50段) | Limit reached (50) |
| `ztr-selection-translate-all` | 翻译全部 | Translate all |
| `ztr-selection-clear` | 清空 | Clear |
| `ztr-selection-clear-confirm` | 清空当前文献的跨区域选区? | Clear multi-region selection for this item? |
| `ztr-section-selection-header` | 跨区域选区 | Multi-region selection |
| `ztr-selection-delete` | 移除此区域 | Remove region |

---

## 7. 实施步骤(M7)

1. **`src/modules/selection.ts`**:SelectionManager 数据类 + joinRegions(纯函数,可单测)
2. **`src/modules/reader.ts`**:弹层加「加入选区」按钮(取消定时器、✓/去重/上限反馈、弹层保持);导出 `getCurrentReader()`
3. **`src/ui/sections.ts`**:注册「跨区域选区」区块(onItemChange 激活条件、订阅 SelectionManager 重绘、翻译全部/清空/删除/跳页)
4. **`src/addon.ts` / `src/hooks.ts`**:装配 SelectionManager 并注入
5. **i18n**:§6.8 文案写入 `mainWindow.ftl`(zh-CN + en-US)
6. **验证**:§8 验收清单逐项过;既有功能回归

---

## 8. 验收标准

**功能**
- [ ] 划选一段 → 弹层出现「翻译」「加入选区」两个按钮
- [ ] 点「加入选区」→ 按钮变「✓ 已加入(1段)」并恢复;侧栏出现「跨区域选区」区块
- [ ] 再划另一段加入 → 列表两行,按文档顺序显示(乱序加入也按文档序)
- [ ] 重复加入同一段 → 按钮提示「已在选区中」,列表不重复
- [ ] 点「翻译全部」→ 「当前翻译」卡片出现完整段落译文(无断行),历史记为一条
- [ ] 区域行点击 → 阅读器跳转到对应页
- [ ] 区域行「×」→ 仅移除该区域
- [ ] 「清空」→ 确认后列表清空,区块完全隐藏(含标题栏)
- [ ] 翻译全部后选区保留(不自动清空)
- [ ] 切换文献:各文献选区独立;无选区文献不显示区块
- [ ] 加入第 51 段被拒并提示
- [ ] 区域边界连字符(如 `transla-` + `tion`)拼接正确
- [ ] 开启划选即译时,点「加入选区」不触发自动翻译

**回归**
- [ ] 单段翻译、快捷键翻译/总结、历史、总结功能不受影响
- [ ] Zotero 9 主测;8.x 冒烟(若 annotation 缺 `sortIndex`/`pageLabel`,走 §6.2 兜底)

---

## 9. 风险与对策

| 风险 | 对策 |
|------|------|
| `params.annotation` 字段(`pageIndex`/`sortIndex`/`pageLabel`)为 zotero/reader 实现细节,非文档化,未来版本可能变动 | 读取处统一做缺省兜底(§6.2);8.x 回归冒烟;字段变动只影响排序精度,不崩溃 |
| 单次跨页拖选最多两页文本(官方 `slice(0, 2)` 截断) | 文档说明:跨多页请分段加入;列表按页展示可核对 |
| `setEnabled` 隐藏行为依赖 ItemPaneManager 实现 | 已在 zotero/zotero 源码核实(7+ 一致);实施时 9.x 实测确认 |
| 无文本层 PDF(扫描件)划选无 text | 现有守卫 `!text return`,按钮不出现,行为不变 |
| 区块初始位置由注册顺序决定(可能在翻译区块下方) | 接受默认位置,用户可在侧栏拖拽;不强制排序 |

---

## 10. 参考

- zotero/reader:`src/pdf/pdf-view.js`(`_getAnnotationFromSelectionRanges`)、`src/common/components/view-popup/selection-popup.js`
- zotero/zotero:`chrome/content/zotero/xpcom/pluginAPI/itemPaneManager.js`(registerSection/setEnabled 语义)、`chrome/content/zotero/elements/itemPaneCustomSection.js`、`chrome/content/zotero/xpcom/reader.js`(`navigate`)
- 本项目:`src/modules/reader.ts`、`src/ui/sections.ts`、`src/modules/tasks.ts`
